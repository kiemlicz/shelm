apiVersion: v1
kind: Service
metadata:
  name: {{ include "programmatic-chart.fullname" . }}
  labels:
    {{- include "programmatic-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "programmatic-chart.selectorLabels" . | nindent 4 }}
